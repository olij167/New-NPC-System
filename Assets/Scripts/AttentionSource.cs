using UnityEngine;

public class AttentionSource : MonoBehaviour
{
    [Tooltip("How much attention this object draws (default is 1). Higher values draw more attention.")]
    public float attentionFactor = 1f;
}
